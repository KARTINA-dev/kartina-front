import * as users from './users';
import * as market from './market';
import * as items from './items';
import * as currency from './currency';

const api = { users, market, items, currency };

export default api;
